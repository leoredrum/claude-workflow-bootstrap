# README.md 更新说明

## 更新时间
2026-05-16

## 更新内容

### 1. 新增章节

#### "## Claude vs Claude-plan"
- 位置: 在"设计取舍"章节之前
- 内容:
  - **claude** (主命令): 日常交互、编程、快速原型开发
  - **claude-plan** (规划工具): 正式开发模式，自动创建 `.project-ai/` 工作目录，结构化项目规划

#### "## Secret 管理规则"
- 位置: 在"Claude vs Claude-plan"章节之后，"设计取舍"之前
- 内容:
  - ⚠️ 安全警告: 永远不要把敏感信息写进 git
  - 禁止项: Cookie/密码/Token/API Keys/私人配置等
  - 推荐做法: 使用 `get-secret` 从 macOS Keychain 读取敏感信息
  - 示例命令: `get-secret github api-token`

### 2. 验证步骤增强

#### "一行命令 (推荐)" 部分
- 新增验证命令组:
  ```bash
  # GitHub CLI 验证
  gh auth status

  # 项目工具验证
  ai-project-init
  claude-plan
  local-coder
  get-secret
  ```

#### "手动逐步" 部分
- 扩展为完整的安装+验证流程
- 保持原有安装步骤，新增第5步验证步骤
- 每个命令都添加了注释说明预期输出

### 3. 安装步骤说明更新

- 保持原有三条安装路径不变
- "让 AI 帮跑" 路径保持原样
- "一行命令 (推荐)" 和 "手动逐步" 路径都增加了验证步骤
- 所有验证命令都包含了预期输出说明

## 文件结构变化

**保持不变**:
- 原有章节顺序和结构
- "含什么"、"升级"等章节
- 所有原有内容的完整性

**新增内容**:
- 2个主要章节 (Claude vs Claude-plan, Secret 管理规则)
- 验证命令说明 (2处)
- 命令预期输出说明

## 技术细节

### 新命令功能说明
1. **claude-plan**: 正式开发模式，自动创建工作目录，用于项目规划和架构设计
2. **local-coder**: 本地 coder 接口（当前为 stub，后续接 oMLX）
3. **get-secret**: 从 macOS Keychain 读取 secret，用于安全的凭证管理

### 文档风格
- 保持原有中文文档风格
- 使用代码块展示命令示例
- 使用 emoji (⚠️、✓) 增强可读性
- 保持技术文档的简洁性和实用性

## 兼容性
- 与现有 README.md 结构完全兼容
- 不影响任何现有功能描述
- 所有新增内容都是对原有文档的增强，不是替代
